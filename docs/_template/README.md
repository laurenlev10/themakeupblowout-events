# Landing page template

The landing-page-builder agent reads `landing.html.tpl` from this folder, substitutes per-event variables, and writes the result to `/docs/{event-slug}/index.html`.

To be filled in next.
